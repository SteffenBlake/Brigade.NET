namespace Brigade.Net.Partie;

// TODO: This shouldnt exist, we should already have the Unit type declared in core

/// <summary>An empty context for routes with no dependencies.</summary>
public sealed class EmptyContext;
